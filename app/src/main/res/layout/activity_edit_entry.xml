<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_new_entry"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ray.pokedb.EditEntry">

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="34dp"
        android:id="@+id/editEntrySpinner"
        android:visibility="gone"/>

    <EditText
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:hint="@string/candy"
        android:fontFamily="casual"
        android:ems="10"
        android:layout_alignParentStart="true"
        android:layout_below="@id/editEntrySpinner"
        android:id="@+id/editEntryCandyInput" />

    <Button
        android:text="@string/submit"
        android:textSize="10pt"
        android:fontFamily="casual"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editEntryCandyInput"
        android:layout_alignParentStart="true"
        android:id="@+id/buttonEditNewEntry"
        android:onClick="buttonEditEntry" />
</RelativeLayout>
